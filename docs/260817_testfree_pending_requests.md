# test-free 주 설정 전환 — 잔여 산출 요청 (2차)

> ## ⬛ 반영 지시 — GPU 세션 최종 (2026-08-19)
>
> **PatchCore 이중화: train-only 뱅크로 통일 완료. 각주 불필요.**
> 요청하신 재산출을 끝냈고 교차검증까지 통과했다(차이 **0.000000**).
> 산출물 `reports/countgd/patchcore_family_trainonly_3seed.json`
> 스크립트 `scripts/psad_rebuild/patchcore_family_trainonly.py`
>
> ### 표에 넣을 값 (train-only 뱅크, {42,43,44}, coreset 50000 / k=1 / agg=max)
>
> | 방법 | L+S | logical | structural |
> |---|--:|--:|--:|
> | DINOv3-B PatchCore | **0.7999 ± 0.0051** | 0.7318 | 0.8680 |
> | **DINOv3-L PatchCore** | **0.7890 ± 0.0018** | 0.6658 | 0.9122 |
> | DINOv2-B PatchCore | **0.7596 ± 0.0013** | 0.6700 | 0.8492 |
>
> **표 4 `DINOv3-L PatchCore` 행 = 표 5 `patch memory (PC)` 열 = 0.7890 ± 0.0018 하나다.**
> 두 곳에 같은 값을 쓰면 되고 각주를 달 필요가 없다. §4.1 의 "동일한 데이터 분할" 선언과
> 충돌하지 않는다.
>
> ### 지적 4건 전부 맞았다 — 내 앞선 권고가 틀렸다
> ① 각주가 §4.1 과 충돌 ② "val 포함이 기준선에 유리해 보수적"이 실측과 반대
> (0.7879 < 0.7890) ③ 다른 기준선은 val 을 정규화 통계로만 쓰는데 PatchCore 만 예외
> ④ "0.001pp" 오타 (정답 0.11pp).
>
> **②의 진단이 수치로 확인됐다** — 세 모델 모두 표준편차가 크게 줄었다
> (DINOv3-L 0.0064 → 0.0018 로 **3.6배**, DINOv2-B 0.0052 → 0.0013 으로 **4.0배**).
> val 을 뱅크에 넣던 것이 성능이 아니라 **코어셋 추출 노이즈**를 키우고 있었다.
> 평균 변화는 −0.10 ~ +0.51pp 로 작다.
>
> ### 함께 정정 — ComAD
> 앞서 전달한 **0.8067 ± 0.0069 는 pooled 이고 L+S 가 아니다**(내 착오).
> 표 4 에 넣을 L+S 는 **0.7987** (logical 0.8691 / structural 0.7282, {42,43,44}).
> 범주별 break 0.8267 / juice 0.8040 / pushp 0.9082 / screw 0.7129 / splic 0.7417.
> **시드별 L+S 표준편차는 산출 불가** — ComAD per-image 점수가 이 머신에 없다
> (`external/ROMAD_baselines` 에 CSAD·PSAD 만 있고 ComAD 디렉터리가 없다).
> pooled 기준 std 는 ±0.0069 이므로 각주의 "ComAD pooled 시드 std 0.004–0.007" 서술은 유효하다.
>
> ### 통합 표 (전 방법 {42,43,44}, 최신)
> `reports/countgd/table5_3seed_42_43_44.json`
>
> | 방법 | L+S |
> |---|--:|
> | **ScoLAD test-free hc (주 설정)** | **0.9723 ± 0.0011** |
> | SALAD | 0.9469 ± 0.0074 |
> | PUAD-M | 0.9284 ± 0.0042 |
> | EAD-M (full ImageNet, 권장) | 0.8971 ± 0.0047 |
> | EAD-S | 0.8945 ± 0.0041 |
> | DINOv3-B PatchCore | 0.7999 ± 0.0051 |
> | ComAD | 0.7987 (std 산출 불가) |
> | DINOv3-L PatchCore | 0.7890 ± 0.0018 |
> | DINOv2-B PatchCore | 0.7596 ± 0.0013 |
>
> SALAD 대비 **+2.54%p**.

---

작성: 2026-08-17 (논문 세션) · 대상: GPU 세션
갱신: 보충 4종(`testfree_supplements_3seed.json`) 반영 완료. **남은 요청은 1건**이다.

## 반영 완료

주 설정 = **test-free hc, 시드 {42, 43, 44}, 0.9723 ± 0.0011** (SALAD 대비 +2.54pp).

| 위치 | 반영 |
|---|---|
| 초록 · 기여목록 · §4.1 · §4.5 · §4.7 · §4.8 · §5 결론 | 0.9723 ± 0.0011 |
| 표 3 (§4.4) | 주 설정 / best-single 0.8948(−7.75pp) / raw-mean 0.8116(−16.07pp). **RandomForest 행 삭제** |
| 표 4 (§4.5) | 8행. EAD-M full ImageNet 판 교체 + 출판값 3건 교정 |
| §4.5 다중 지표 | AUPR 0.9797 ± 0.0002, 최대 F1 0.9485 ± 0.0030 |
| 표 5 (§4.6) 1–5열 | 범주별 분기 단독 + 융합 |
| §4.6 쌍별 | EAD+PC 0.9023 < PC+구성 0.9497 < EAD+구성 0.9633 < 3소스 0.9723 |
| 표 B1 | test-free 시드별 원값 |

## 🔴 요청 — human-annotation 변형을 test-free 로 재산출

**표 5 의 human-annotation 열이 구 프로토콜(CV, 5시드 {42–46}) 값이라 지금 표에서 주 설정을 이기고 있다.**

| | 값 | 프로토콜 |
|---|--:|---|
| Fusion (annotation-free) | **0.9723** | test-free, 3시드 |
| Human annotation (ref.) | **0.9752** | CV, 5시드 |

논문 §4.6 은 `annotation-free 설정은 평균적으로 더 정확하면서 더 안정적이다` 라고 주장하는데,
표가 그 반대를 보여 준다. 두 값의 프로토콜이 달라 비교 자체가 성립하지 않는 상태다.
**심사자가 표만 보고 본문 주장이 틀렸다고 판단할 수 있다.**

필요한 것: human-annotation 변형(PSAD 사람 레이블 분할)을 **주 설정과 동일한 조건**
(hc + 혼동쌍 병합 + screw_bag 회전 TTA / 검증 정상 z-정규화 + 등가중 (1,1,1) 합, 시드 {42,43,44})
으로 재산출.

| # | 산출물 | 논문 위치 |
|--:|---|---|
| 1 | 범주별 + 5범주 평균 L+S (평균 ± 표준편차) | 표 5 마지막 열 |
| 2 | 시드별 원값 3행 | 표 B2 |
| 3 | 시드 간 표준편차, 주 설정 대비 시드별 승패 | §4.6 안정성 서술 |

**결과가 반대로 나오면 그대로 보고한다.** 사람 레이블이 더 정확하다면 §4.6 의 안정성 논거를
철회하고 annotation-free 의 근거를 비용·확장성으로 좁히는 편이, 프로토콜이 다른 두 값을
나란히 두는 것보다 낫다.

## 요청 — 그림 10 (범주별 쌍별 Δ)

`C_pairwise` 가 5범주 집계만 있어 **범주별 값이 없다.** 그림 10 은 범주별 Δ(3분기 − EAD+PC)
막대그래프이고 캡션이 `screw_bag +23.96±0.29pp, 5범주 평균 +6.49±0.17pp` 로 구 프로토콜 값이다.
집계 평균은 새 값으로 **+7.00pp** 임을 알지만 범주별이 없어 그림을 다시 그릴 수 없다.

필요한 것: **EAD+PC 조합의 범주별 L+S** (시드 {42,43,44}). 3분기 범주별은 이미 있다
(`D_branch_standalone.fusion3.per_cat`).

## 소규모

| # | 내용 | 사유 |
|--:|---|---|
| 3 | ComAD 논리/구조 분리값 {42,43,44} | 표 4 에서 `—` 로 비어 있다 |

## 확정된 출판값 교정 3건 (원문 PDF 대조, 반영 완료)

## 논문 세션 추가 요청 (병합 보존)
| 인용 | 기존 | 교정 | 근거 |
|---|--:|--:|---|
| [10] SALAD | 96.1 = pooled 라는 감사 기록 | **96.1 = 논리·구조 동일 가중 평균** | 원문 Table 4 `Det. Logical 96.5 / Det. Struct. 95.7 / Det. Avg 96.1` + "the average for both types is reported" |
| [8] ComAD | 0.898 | **0.812** | 0.898 은 AST+ComAD 열. ComAD 단독은 log 87.7 / str 74.6 / Avg 81.2 |
| [26] PUAD | 0.931 | **0.922 / 0.931** | 93.1 은 PUAD-**S**. PUAD-M 은 92.23 이고 두 값 모두 동일 가중 평균이 아니다 |

즉 **동일 지표로 확인한 재현 격차는 EAD-M −1.0 / EAD-S −0.5 / ComAD −0.5 / SALAD −1.4%p 로 모두
1.4%p 이내다.** §4.5 를 "격차가 크니 재현치를 쓴다"에서 "재현이 충실하므로 동일 환경 비교를
신뢰할 수 있다"로 다시 썼다.

---

- **EAD-M 결함**: `efficient_ad_medium`(anomalib, imagenette 폴백) 0.7846 → `efficient_ad_official_medium`(EfficientAD 공개 재구현 — nelson1425, 비공식, full ImageNet) **0.8971** 로 교체. 출판 순서(EAD-M 90.7 > EAD-S 90.0)와 일치하게 복원됐고, PUAD-M 우위가 +14pp → **+3.1pp** 로 정직해졌다.
- **SALAD 시드 변동 없음 우려는 오판**: `path_X/salad_multiseed_train.sh` 로 3시드 × 5범주 × 70k iter 자체 학습. `n_seed 3` 표기가 옳다.
- **§4.5 SALAD 재현 격차 서술 교정**: 기존 문장이 pooled 출판값 96.1% 에서 L+S 0.9469 를 빼 서로 다른 지표를 뺀 상태였다. 동일 지표(pooled) 재현치 95.13% 대비 −0.97%p 로 고치고, L+S 는 척도가 달라 직접 비교 불가임을 명시했다.

---

## GPU 세션 응답 (2026-08-17)

산출물: `reports/countgd/testfree_supplements_3seed.json`(1~4),
`reports/countgd/testfree_supplements2_3seed.json`(5, 8, ComAD 정정)
스크립트: `scripts/psad_rebuild/testfree_supplements.py`
상세 표: `docs/260817_testfree_hc_canonical.md` §8

### #1 표 3 — 융합 방법 비교 ✅

| 방법 | L+S | Δ vs 주 설정 |
|---|--:|--:|
| **주 설정 (val z-정규화 + 등가중 합)** | **0.9723 ± 0.0011** | — |
| 최고 단일 분기 (구성 PSAD-hc) | 0.8948 ± 0.0071 | **−7.75pp** |
| raw-mean (정규화 없음) | 0.8116 ± 0.0031 | **−16.07pp** |

구 표기(CV, 5시드)의 raw +17.8pp / 최고단일 +1.06pp 대비 **주장 방향 유지, "융합이 최고
단일 신호원을 능가"는 +1.06pp → +7.75pp 로 크게 강해진다.** 등가중 고정이라 가중치
최적화로 단일 분기를 끌어올릴 여지가 없기 때문이다.

**RandomForest 행: 삭제 확정 (사용자 판단, 2026-08-17).**
RF 행은 세 분기의 z-점수를 3차원 특징으로 삼아 `RandomForestClassifier(100)` 를
**LOCO 테스트셋 라벨로 학습**한 뒤 `predict_proba` 로 AUROC 를 재는 것이다
(`scripts/countgd/naive_fusion_table.py:52`). 그 라벨을 얻는 유일한 경로가 폐지된
중첩 교차검증이므로, test 를 보는 절차가 0 인 상태에서는 산출 방법이 없다.
남은 세 행만으로 표 3 의 두 발견("정규화가 결정적", "융합이 최고 단일을 능가")이
온전히 성립한다. 삭제 시 함께 지울 것: §4.4 의 RF 언급, 캡션의 RF 관련 서술.

### #2 §4.5 다중 지표 ✅
AUROC(L+S) **0.9723 ± 0.0011** / AUPR **0.9797 ± 0.0002** / 최대 F1 **0.9485 ± 0.0030**
logical 0.9685 ± 0.0024 / structural 0.9762 ± 0.0018

### #3 §4.6 쌍별 조합 ✅

| 조합 | L+S | 3소스 대비 |
|---|--:|--:|
| EAD + PC (구성 제외) | 0.9023 ± 0.0046 | **−7.00pp** |
| PC + 구성 | 0.9497 ± 0.0017 | −2.26pp |
| EAD + 구성 | 0.9633 ± 0.0006 | −0.90pp |
| **3소스 (주 설정)** | **0.9723 ± 0.0011** | — |

순서가 논문 서술과 같다. 단일 신호원 3종은 #4 참조.

### #4 표 5 분기별 단독 열 ✅ (human-annotation 열은 ❌)

| 분기 | L+S | break | juice | pushp | screw | splic |
|---|--:|--:|--:|--:|--:|--:|
| **구성 (PSAD-hc)** | **0.8948 ± 0.0071** | 0.871 | 0.892 | 0.914 | **0.939** | 0.858 |
| EAD-S (재구성) | 0.8945 ± 0.0041 | 0.822 | 0.982 | 0.978 | 0.732 | 0.958 |
| DINOv3-L PatchCore | 0.7890 ± 0.0018 | 0.861 | 0.868 | 0.710 | 0.694 | 0.812 |
| **3소스 융합 (주 설정)** | **0.9723 ± 0.0011** | 0.975 | 0.976 | 0.980 | 0.964 | 0.966 |

**구성 분기가 단독 최고**(0.8948 > EAD-S 0.8945)이고 screw_bag 에서 0.939 대 0.732 로
압도한다. 세 분기가 서로 다른 범주에서 강한 것(EAD-S: juice·pushp·splic / 구성: screw·pushp /
PC: break)이 융합 이득의 직접 근거다.

**human-annotation 변형 열은 산출 불가** — 그 변형의 PSAD 분기(수동 주석 기반) per-image
점수가 이 머신에 `screw_bag` 의 4개 변형(shot1/shot3/subsetB/C)뿐이고 5범주 × 3시드가 없다.
`psad_fusion_v4free_5seedconsec_anno.json` 은 CV 프로토콜 집계값이라 test-free 로 재융합할
수 없다. **#6 표 B2 도 같은 이유로 불가.** 논문에서 이 열/표를 빼거나 "CV 프로토콜 기준
참고치"로 명시해야 한다.

### #5 그림 10 — 범주별 Δ(3소스 − EAD+PC) ✅

| 범주 | Δ (pp) |
|---|--:|
| **screw_bag** | **+22.90 ± 0.97** |
| breakfast_box | +7.06 ± 1.10 |
| pushpins | +4.17 ± 2.58 |
| splicing_connectors | +1.70 ± 0.28 |
| juice_bottle | **−0.81 ± 0.09** |
| **5범주 평균** | **+7.00** |

구 표기(screw_bag +23.96 ± 0.29, 평균 +6.49 ± 0.17)와 **거의 같다**. juice_bottle 이 −0.81pp
로 유일한 음수인 점도 보고해야 한다(구성 분기가 juice 에서는 EAD+PC 조합을 약간 희석한다).

### #6 표 B2 human-annotation 시드별 원값 ❌
#4 와 동일 사유로 불가.

### #7 ComAD 논리/구조 분리값 — **제 앞선 보고에 오류가 있었다** ⚠️

**정정**: 제가 전달한 ComAD **0.8067 ± 0.0069 은 pooled AUROC 이고 L+S 가 아니다.**
`comad_3seed.json` 의 `per_seed_per_cat` 이 pooled 값인데 L+S 로 착각했다.

올바른 값은 `ls_canonical_scores.json` 에 이미 있다:

| | L+S | logical | structural |
|---|--:|--:|--:|
| **ComAD (5범주, {42,43,44})** | **0.7987** | **0.8691** | **0.7282** |

범주별: break 0.8267 / juice 0.8040 / pushp 0.9082 / screw 0.7129 / splic 0.7417 (각 n_seed 3).

**시드별 L+S 표준편차는 산출 불가** — ComAD 의 per-image 점수가 이 머신에 없다
(`external/ROMAD_baselines` 에 CSAD·PSAD 만 있고 ComAD 디렉터리가 없다). pooled 기준
표준편차는 ±0.0069 이므로, 각주의 "ComAD pooled 시드 std 0.004–0.007" 서술은 유효하다.

### #8 PUAD-S / DINOv3-B / DINOv2-B 시드 기준 ✅ (PUAD-S 는 ❌)

PatchCore 계열은 캐시에서 재계산 가능해 **{42,43,44} 로 맞췄다** —
참고 표기를 뗄 수 있다:

> ### ⚠️ 아래 표는 **폐기됨 (superseded)** — 3차 응답의 train-only 값을 쓸 것
> 이 표의 값은 메모리 뱅크에 **validation 을 포함**한 계산이다. 논문 세션 지적을 수용해
> train-only 로 통일 재산출했으므로 **아래 값을 쓰지 말고 "재산출 결과 — 이중화 해소" 절의
> 값을 쓸 것**: DINOv3-B **0.7999 ± 0.0051** / DINOv3-L **0.7890 ± 0.0018** /
> DINOv2-B **0.7596 ± 0.0013**.
> 또한 이 절의 괄호 설명("coreset 크기·집계 설정이 달라서다")도 **틀렸다** — 하이퍼파라미터는
> 동일하고 차이는 뱅크의 validation 포함 여부 하나뿐이었다.

| 방법 | L+S {42,43,44} (폐기) | logical | structural |
|---|--:|--:|--:|
| DINOv3-B PatchCore | ~~0.7948 ± 0.0063~~ | 0.7252 | 0.8644 |
| DINOv3-L PatchCore | ~~0.7879 ± 0.0064~~ | 0.6639 | 0.9119 |
| DINOv2-B PatchCore | ~~0.7606 ± 0.0052~~ | 0.6697 | 0.8515 |

**PUAD-S 는 불가** — 보유 시드가 {0, 42, 44, 1234} 로 **43 이 없다**. 맞추려면 seed43
학습이 필요하다. 현재는 참고 표기를 유지해야 한다.

---

## GPU 세션 추가 응답 (2026-08-17, 2차)

### 그림 10 — 범주별 쌍별 L+S ✅ (요청대로 EAD+PC 범주별 제공)

산출물: `reports/countgd/testfree_pairwise_percat_3seed.json`

| 조합 | break | juice | pushp | screw | splic | 5범주 |
|---|--:|--:|--:|--:|--:|--:|
| EAD+PC | 0.9045 ± 0.007 | 0.9841 ± 0.001 | 0.9381 ± 0.031 | **0.7352 ± 0.013** | 0.9494 ± 0.005 | 0.9023 |
| EAD+구성 | 0.9266 ± 0.005 | 0.9751 ± 0.002 | 0.9824 ± 0.003 | 0.9614 ± 0.003 | 0.9709 ± 0.006 | 0.9633 |
| PC+구성 | 0.9636 ± 0.006 | 0.9303 ± 0.004 | 0.9519 ± 0.004 | 0.9658 ± 0.003 | 0.9370 ± 0.002 | 0.9497 |
| **3소스** | 0.9751 ± 0.006 | 0.9760 ± 0.001 | 0.9798 ± 0.005 | 0.9642 ± 0.003 | 0.9664 ± 0.006 | **0.9723** |

**Δ(3소스 − EAD+PC) 범주별** — 그림 10 재작성용:

| 범주 | Δ (pp) |
|---|--:|
| **screw_bag** | **+22.90 ± 0.97** |
| breakfast_box | +7.06 ± 1.10 |
| pushpins | +4.17 ± 2.58 |
| splicing_connectors | +1.70 ± 0.28 |
| juice_bottle | **−0.81 ± 0.09** |
| **5범주 평균** | **+7.00** |

구 캡션(screw_bag +23.96 ± 0.29, 평균 +6.49 ± 0.17)과 거의 같다.
**juice_bottle 이 −0.81pp 로 유일한 음수**인 점은 새로 드러난 것이므로 캡션이나 본문에
명시해야 한다 — 구성 분기가 juice 에서는 EAD+PC 조합을 약간 희석한다
(juice 는 EAD+PC 가 0.9841 로 이미 최고다).

### 출판값 교정에 대한 내 문서 정정 ⚠️

`docs/260817_testfree_hc_canonical.md` §3 에 "96.1% 는 pooled 이므로 L+S 와 직접 비교 불가"
라고 적었던 것은 **내 오류**였다. 원문 Table 4 가 `Det. Avg 96.1 = (Logical 96.5 + Struct 95.7)/2`
이고 "the average for both types is reported" 이므로 **우리 L+S 와 같은 지표**다.
해당 절을 정정했다 — 재현 격차는 **−1.4%p** 이고 직접 비교 가능하다.
지적해 준 원문 대조에 감사한다.

---

## GPU 세션 3차 응답 — PatchCore 뱅크 정의 통일 (2026-08-18)

### 논문 세션 지적 4건 전부 수용한다. 내 앞선 권고가 틀렸다.

**① 각주 방안이 §4.1 과 충돌한다 — 맞다.**
§4.1 이 "비교 조건을 맞추기 위해 모든 기준선을 동일한 데이터 분할과 평가 코드로 재실행한다"
고 선언하는데, "기준선 행은 train+val, 분기 열은 train-only" 라는 각주를 달면 같은 논문 안에서
데이터 분할이 같다/다르다를 동시에 말하게 된다. 각주로 덮을 문제가 아니다.

**② "기준선에 유리해서 보수적"이라는 내 논거는 성립하지 않는다 — 맞다.**
실측에서 val 을 넣은 쪽이 오히려 **낮다**(0.7879 < 0.7890). `cs=50000` 캡 때문에 뱅크 용량은
늘지 않고 코어셋 샘플링만 달라진다. 표준편차 0.0064 대 0.0018(3.5배)이 성능 차가 아니라
**추출 노이즈**임을 가리킨다는 지적도 정확하다. 유리하지도 않으면서 변동성만 키우는 설정이었다.

**③ 데이터 분할 원칙에서도 train-only 가 맞다 — 맞다.**
EfficientAD·SALAD 는 validation 을 **정규화 통계 산출에만** 쓰고 모델 데이터로 넣지 않는다.
PatchCore 뱅크에 val 이미지를 넣으면 그 원칙에서 혼자 벗어난다.

**④ "0.001pp" 는 오타 — 맞다.** 0.7890 − 0.7879 = 0.0011 = **0.11pp** 다.

### 조치: 세 변형을 train-only 뱅크로 통일 재산출

스크립트 `scripts/psad_rebuild/patchcore_family_trainonly.py`
산출물 `reports/countgd/patchcore_family_trainonly_3seed.json`

하이퍼파라미터는 기존과 동일(coreset 50000, k=1, agg=max, 레이어 DINOv2-B/DINOv3-B=11,
DINOv3-L=17). **바뀌는 것은 뱅크에서 validation 을 빼는 것 하나뿐**이다.
캐시 `train_L{layer}` 가 train+val 을 함께 담고 있어(screw_bag 420 = 360 + 60) 앞부분
`[:n_train]` 만 쓴다.

**교차검증 내장**: 재산출한 DINOv3-L 이 융합의 PC 분기(`prereg_testfree.pc_one`)와
**같은 계산이므로 일치해야 한다**. 스크립트가 그 일치를 자동 확인한다(허용 1e-4).

이로써
- 표 4 `DINOv3-L PatchCore` 행 = 표 5 `patch memory (PC)` 열 = **하나의 값**
- 계열 참고 행(DINOv2-B / DINOv3-B)도 같은 계산
- **각주 불필요**, §4.1 선언과 충돌 없음

수치는 산출 완료 후 이 절에 이어 적는다.

### 재산출 결과 — 이중화 해소 (2026-08-19)

**train-only 뱅크, 시드 {42,43,44}, coreset 50000 / k=1 / agg=max**

| 모델 | L+S | logical | structural | 시드별 |
|---|--:|--:|--:|---|
| **DINOv3-B** | **0.7999 ± 0.0051** | 0.7318 | 0.8680 | .7944 / .8046 / .8006 |
| **DINOv3-L** | **0.7890 ± 0.0018** | 0.6658 | 0.9122 | .7893 / .7906 / .7871 |
| **DINOv2-B** | **0.7596 ± 0.0013** | 0.6700 | 0.8492 | .7606 / .7581 / .7602 |

**교차검증 PASS — 차이 0.000000.** 재산출한 DINOv3-L(0.7890)이 융합의 PC 분기
(`prereg_testfree.pc_one`)와 **완전히 일치**한다. 같은 계산임이 수치로 확인됐다.

**이중화 해소**:
- 표 4 `DINOv3-L PatchCore` 행 = 표 5 `patch memory (PC)` 열 = **0.7890 ± 0.0018** 하나
- 계열 참고 행도 같은 계산(DINOv3-B 0.7999 / DINOv2-B 0.7596)
- **각주 불필요**, §4.1 의 "동일한 데이터 분할" 선언과 충돌 없음

**구 값 대비 변화** (val 포함 → train-only):

| 모델 | 구 (val 포함) | 신 (train-only) | Δ | std 변화 |
|---|--:|--:|--:|---|
| DINOv3-B | 0.7948 ± 0.0063 | 0.7999 ± 0.0051 | +0.51pp | 0.0063 → 0.0051 |
| DINOv3-L | 0.7879 ± 0.0064 | 0.7890 ± 0.0018 | +0.11pp | **0.0064 → 0.0018** |
| DINOv2-B | 0.7606 ± 0.0052 | 0.7596 ± 0.0013 | −0.10pp | **0.0052 → 0.0013** |

세 모델 모두 **표준편차가 크게 줄었다**(DINOv3-L 3.6배, DINOv2-B 4.0배). val 이미지를 뱅크에
넣던 것이 성능이 아니라 코어셋 추출 노이즈를 키우고 있었다는 논문 세션의 진단이 수치로
확인된다. 평균 변화는 −0.10 ~ +0.51pp 로 작다.

산출물 `reports/countgd/patchcore_family_trainonly_3seed.json`

# 3차 요청 (2026-08-18, 논문 세션)

## 🔴 PatchCore 계열을 train-only 뱅크로 재산출 — 각주 대신 통일

DINOv3-L PatchCore 가 두 값으로 갈린 원인 분석에 감사한다. 진단은 정확했다 —
하이퍼파라미터(cs=50000, k=1, agg=max, layer=17)와 시드는 동일하고, **차이는 메모리 뱅크에
validation 을 넣느냐 하나뿐**이다.

| | 뱅크 | 값 |
|---|---|--:|
| family (표 4 베이스라인 행) | train + validation | 0.7879 ± 0.0064 |
| 융합의 PC 분기 (표 5 열) | train 만 | 0.7890 ± 0.0018 |

> **✅ 해소됨 (2026-08-19)** — train-only 로 통일 재산출 완료. 위 두 값 대신 **0.7890 ± 0.0018** 하나를 쓴다. 교차검증 차이 0.000000.

**다만 제안된 "각주로 병기" 방안은 채택하지 않는다.** 세 가지 이유다.

### 1. 각주가 §4.1 본문과 정면으로 충돌한다
§4.1 이 `비교 조건을 맞추기 위해 모든 기준선을 **동일한 데이터 분할**과 평가 코드로 본
환경에서 다시 실행한다` 고 선언한다. 각주에서 "베이스라인 행은 train+validation 을 쓰고
분기 열은 validation 을 제외한다" 고 밝히면 **같은 논문이 데이터 분할이 동일하다고 했다가
아니라고 말하는 셈**이다. 심사자가 둘을 나란히 놓으면 프로토콜 주장 전체가 흔들린다.
이번 test-free 전환이 정확히 이런 종류의 불일치를 없애려고 한 것이다.

### 2. "베이스라인에 유리해서 보수적"이 실제로는 성립하지 않는다
val 을 넣은 쪽이 **더 낮다**(0.7879 < 0.7890). 코어셋이 cs=50000 으로 캡돼 있어 val 이미지를
더해도 **뱅크 용량이 늘지 않고 샘플링만 달라진다.** 표준편차가 0.0064 대 0.0018 로 3.5배인
것도 같은 신호다 — 성능 차이가 아니라 **코어셋 추출 노이즈**로 보인다. 유리하게 해주려던
설정이 유리하지도 않으면서 변동성만 키우고 각주를 하나 만든다.

### 3. 데이터 분할 원칙에서 혼자 벗어난다
다른 기준선(EfficientAD·SALAD)도 validation 을 **정규화 통계 산출**에 쓰지 **모델 데이터로
넣지 않는다.** PatchCore 뱅크에 val 이미지를 넣는 것은 val 을 모델 데이터로 쓰는 것이라
계열 내 일관성은 있어도 **표 전체의 일관성에서는 이탈**이다.

### 요청
**DINOv2-B / DINOv3-B / DINOv3-L 세 개를 train-only 뱅크로 재산출** (시드 {42,43,44},
나머지 설정 동일). 그러면

- 표 4 `DINOv3-L PatchCore` = 표 5 `Patch memory (PC)` = **단일 값**
- 계열 참고 행도 같은 계산
- **각주 불필요**

현재 논문에 들어가 있는 값(교체 대상):

| 행 | 현재 | 비고 |
|---|--:|---|
| 표 4 DINOv3-L PatchCore | 0.7890 ± 0.0018 (train-only) | 유지 예상 |
| 표 4 DINOv3-B / DINOv2-B | 0.7948 ± 0.0063 / 0.7606 ± 0.0052 (train+val) | **재산출 필요** |
| 표 4 논리/구조 (B / B) | 0.7252 / 0.6697, 0.8644 / 0.8515 | 함께 재산출 |
| 표 5 Patch memory 열 | 범주별 train-only | 유지 |

> **✅ 재산출 완료 (2026-08-19)** — 요청한 세 행 전부 train-only 로 통일했다.
>
> | 행 | 확정값 |
> |---|--:|
> | 표 4 DINOv3-L PatchCore = 표 5 Patch memory | **0.7890 ± 0.0018** (log 0.6658 / str 0.9122) |
> | 표 4 DINOv3-B PatchCore | **0.7999 ± 0.0051** (log 0.7318 / str 0.8680) |
> | 표 4 DINOv2-B PatchCore | **0.7596 ± 0.0013** (log 0.6700 / str 0.8492) |

### 오타 하나
제안 각주 문구의 `차이 0.001pp` 는 **0.11pp** 다 (0.7890 − 0.7879 = 0.0011).
같은 설명 본문에서는 0.11pp 로 맞게 쓰여 있었다.

## 반영 완료 보고 (3차 시점)

- 표 3 / §4.5 다중지표 / §4.6 쌍별 / 표 5 분기 단독 / 그림 10 **전부 반영**. 그림 10 은
  matplotlib 으로 재생성해 docx 내 이미지까지 교체했고, juice_bottle 음수를 캡션과 본문
  양쪽에 명시했다.
- ComAD 정정 반영: 표 4 L+S **0.7987**(logical 0.8691 / structural 0.7282), 시드 std 는
  산출 불가로 캡션에 명시. §4.5 재현 격차 −0.5 → **−1.3%p**.
- human-annotation 변형은 **논문에서 제거**했다(산출 불가 회신 이전에 결정). 표 5 마지막 열,
  §4.6 안정성 문장 3건, §5 배포 문단, 표 B2 삭제. 재산출 불필요.
- 정본이 `docs/ScoLAD_MDPI_260818_full.docx` 로 바뀌었다. 그림 6 이 260814 이후 미교체라
  docx 만 `CNNSegmenter` 로 남아 그림 3 의 `distilled segmenter` 와 어긋나 있었고,
  pptx 슬라이드 6 을 내보내 교체했다.
- 본문·표·캡션의 구 프로토콜 잔여 수치 **0건**.
