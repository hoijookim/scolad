# 260822 — L17 선택 근거 전수 조사

조사: 2026-08-22 · **머신 5090** · 정본 커밋 `2ad789f`
계기: 요청 ⑦(`260822_l17_coreset_provenance_results.md`)이 "test 로 골랐다"까지 밝혔으므로,
그 앞뒤로 **근거가 존재하는지**를 레포 전체에서 확인한다.

관련 재현 검증: `docs/260822_5090_rerun_verification.md`

---

## 결론

**원고에는 지금 L17 의 근거가 한 줄도 없다. 있었는데 260815 에 삭제됐다.**
그리고 이 판정은 **데이터 부재의 산물이 아니다** — 코드와 커밋된 산출물을 읽어서 나온 것이고,
데이터가 갖춰진 5090 에서 재실행해도 같다.

---

## 1. 판정 근거물은 전부 git 추적 상태다

계층 판정에 필요한 것은 재계산이 아니라 **읽기**다. 아래 6개가 모두 추적 중이므로,
대용량 바이너리(캐시·npz)를 받지 못한 머신도 동일한 근거를 갖는다.

```
scripts/direction_I/I_dinov3_uniform.py                        추적됨
scripts/direction_I/I_dinov3_L_uniform.py                      추적됨
reports/direction_I/I_dinov3_L_uniform/I_dinov3_uniform.json   추적됨
reports/countgd/d7_pc_sensitivity.json                         추적됨
reports/countgd/PREREG_branch_improvement.md                   추적됨
docs/260531_performance_validation_protocol.md                 추적됨
```

JSON 에 `"best_cfg": "L17_cs100k_k1_max"`, `"best_loco": 0.9683` 이 **문자 그대로** 적혀 있다.

추가로 5090 에는 18GB DINOv3-L 캐시와 LOCO 전체가 있어 `layer_choice_testfree_check` 를 실제로
재실행했고, L17 헤드라인이 1e-9 이내로 일치했다. **데이터 유무가 판정을 바꾸지 않는다.**

---

## 2. 후보 집합 {L8, L17, L23} 은 근거가 있다 (test 무관)

`scripts/extract_dinov2_l_336.py:24`

```python
LAYERS = [8, 17, 23]  # L8 (early), L17 (mid), L23 (last)
```

24블록 ViT-L 에서 **얕은/중간/마지막을 하나씩 뽑는 계층 위계 표본**이다. 성능을 보고 만든 집합이
아니므로 이 부분은 방어된다.

---

## 3. 그 안에서 L17 을 고른 것은 test 다

`I_dinov3_L_uniform.py` → `I_dinov3_uniform.py` 가 `X_te = cache["test_L{layer}"]` 를 채점하고
그 위 StratifiedKFold nested CV 로 `mean_fusion` 최댓값을 고른다. 12개 격자 전수:

| cfg | mean_fusion (LOCO test) |
|---|--:|
| **L17_cs100k_k1_max** | **0.96830** ← 선택 |
| L17_cs50k_k1_max | 0.96784 |
| L17_cs100k_k1_top_1pct | 0.96557 |
| L17_cs50k_k1_top_1pct | 0.96447 |
| L23_cs100k_k1_max | 0.96385 |
| L23_cs50k_k1_max | 0.96351 |
| L23_cs50k_k1_top_1pct | 0.96346 |
| L23_cs100k_k1_top_1pct | 0.96259 |
| L8_cs100k_k1_max | 0.96239 |
| L8_cs50k_k1_max | 0.96095 |
| L8_cs50k_k1_top_1pct | 0.96085 |
| L8_cs100k_k1_top_1pct | 0.96016 |

한 설정 **안에서는** 정직한 CV 였지만, 설정 **간** 순위를 test 로 매긴 이상 계층은 test 가 정했다.

`260531_performance_validation_protocol.md:30` 이 이 위험을 **사전에 지목**해 두었다:

> 현재 의심: 96.40 baseline config, DINOv3 backbone(S/B/L)+layer(L9/L17/L23)+coreset+k+agg 선택이
> test LOCO로 이뤄졌을 가능성 → -0.42pp leakage 재발 위험.

지목된 채로 test-free 전환(260815~17) 때 재검증되지 않았고, 260822 요청 ⑦ 에서야 확인됐다.

---

## 4. 원고에 기전적 근거 문장이 있었는데 삭제됐다

v14(260704)~v30(260805) 및 MDPI 260812판까지 §3.1.2 에 있었다:

> 중간 계층을 택한 것은, **얕은 계층은 의미를 충분히 담지 못하고 깊은 계층은 공간 해상도와 국소성을
> 잃어 패치 단위 이상 위치 파악이 어렵기 때문**이다.

| 판본 | 근거 문장 |
|---|---|
| `ScoLAD_paper_v30.docx` (260805) | 있음 |
| `ScoLAD_MDPI_260808_s1_s3.docx` | 있음 |
| `ScoLAD_MDPI_260812_full.docx` | 있음 |
| **`ScoLAD_MDPI_260815_full.docx`** | **없음** |
| 260817 · 260819 · 260821(정본) · 260822_ecomm | 없음 |

**260815 주 설정 test-free 전환 커밋(`71ed106`) 구간에서 사라졌다.** 현행 정본 §3.1.2 는 서술만
남아 있다 — *"입력 해상도 336에서 ViT의 중간 계층(L17)이 산출하는 패치 토큰 격자를 특징으로 쓴다."*

삭제 자체는 방향이 맞았을 수 있다(인용도 측정도 없는 단언이었다). 문제는 **대체 근거 없이 비었다**는 것이다.

---

## 5. 부록 C 는 별도의 test 기반 계층 선택을 스스로 선언한다

정본 부록 C:

> 각 backbone에 대해 … image-level AUROC를, **구조 AUROC(추첨 5회 평균)가 가장 높은 계층**에서 …
> 보고한다(표 C1). 이 계층 선택은 계층 효과를 통제하기 위한 것이며, **주 설정이 쓰는 L17(3.1.2절)은
> 이와 무관하게 고정되어 있다.**

앞부분은 범위가 표 C1 로 한정된 공개 선언이라 정직하다. 뒷문장 **"이와 무관하게 고정되어 있다"** 는
사실과 어긋난다 — L17 도 test 스윕에서 나왔다. 문구 조정이 필요하다.

---

## 6. 사후 test-free 재확인은 성립한다 — 다만 2/3 다

요청 ⑦ 이 PREREG 의 val 기준을 세 계층에 적용했다. **5090 재실행 결과:**

| 계층 | val 꼬리 평균 | val 꼬리 최대 | val CV | (참고) test 융합 | (참고) PC 단독 |
|---|--:|--:|--:|--:|--:|
| L8 | 3.5833 | 4.7616 | **0.0692** | 0.9656 | 0.7678 |
| **L17** | **3.4306** | **4.2408** | 0.0815 | **0.9723** | 0.7890 |
| L23 | 5.4975 | **10.5632** | 0.1039 | 0.9670 | **0.8139** |

- **꼬리는 평균·최대 둘 다 L17 이 이긴다.**
- **val CV 열에서는 L8 이 이긴다.** PREREG 판정 표(`PREREG_branch_improvement.md` §E2 판정 지표)는
  최대 |z_robust| 와 val CV **두 열**이므로, 그 표를 그대로 적용하면 결과는 3/3 이 아니라 **2/3** 이다.

방어는 된다 — PREREG 가 지표를 꼬리로 옮긴 **이유 자체가 꼬리에 특정된 기전**이기 때문이다:

> 해악은 면적 편차 자체가 아니라 **PSAD 점수의 val 꼬리**다 — val 이상치 한 장이 표준편차를 부풀리면
> 그 분기의 test z-점수가 통째로 줄어들어 사실상 분기가 꺼진다.

CV 는 보조 열이고, L8 의 CV 우위(0.0123)는 L23 의 꼬리 열위(10.56)에 비하면 미미하다.
그래도 **먼저 밝히지 않으면 심사자가 같은 표의 다른 열로 L8 을 꺼낼 수 있다.**

### 단서 — 기준 적용은 사후다

계층 선택은 사전등록 항목이 없었고, PREREG 의 val 꼬리 기준은 **구성 분기 E계열(하드/소프트 마스크
채택)용**으로 등록된 것이다. "원하는 답을 주는 기준을 나중에 골랐다"는 반박이 가능하다.
반박을 줄이는 사실 둘: 기준을 이 건을 위해 만들지 않았고(PREREG 원문 그대로), L17 이 꼬리의
평균·최대를 **둘 다** 만족한다.

### 한계를 잘한 것으로 바꿀 여지

**L23 이 분기 단독으로는 최고(0.8139 > L17 0.7890)** 다. 단독 성능으로 골랐다면 L23 을 택했고
융합은 더 나빴을 것이다(0.9670 < 0.9723). val 기준과 융합이 **단독 성능과 반대 방향으로** 일치하는
독립 사례이므로, §4.4 논지("분기는 혼자 잘하는 것보다 서로 보완하는 것이 중요")를 뒷받침한다.

---

## 7. 교차 백본 일관성 — 보강 증거

`direction_I` 의 네 스윕이 **독립적으로 상대깊이 0.70~0.75 를 고른다**:

| backbone | 블록 | 선택 계층 | 상대깊이 | best (test) |
|---|--:|---|--:|--:|
| DINOv3-L/16 | 24 | L17 | 0.708 | 0.9683 |
| DINOv3-B/16 | 12 | L9 | 0.750 | 0.9656 |
| DINOv3-S/16 | 12 | L9 | 0.750 | 0.9627 |
| DINOv2-L/14 | 24 | L17 | 0.708 | 0.9650 |

각 스윕이 test 기반이므로 **개별 값은 근거가 못 된다.** 그러나 계열(v2/v3)과 규모(S/B/L)를 가로질러
같은 상대깊이가 나온다는 **패턴**은 §4 에서 삭제된 기전적 주장("중간 계층")과 방향이 같다.
`pc_backbone_swap.py` 가 이미 이 상대깊이 규칙(0.708 에 가장 가까운 보유 계층)을 채택하고 있어
논문 안에서 규칙이 일관된다.

---

## 8. 함께 치울 것 — `d7_pc_sensitivity`

```python
# G2: PC 소스 config 민감도 (provenance 방어) — layer {L8,L17,L23} × coreset {20k,50k,100k} × k {1,5}
...
Xte = c[f"test_{layer}"].astype("f4")                                   # 25행
for tr,te in StratifiedKFold(5,shuffle=True,random_state=42+r).split(idx,labels):   # 47행
```

헤더가 **"provenance 방어"** 인데 검사 자체가 test 기반이다. 이름이 방어인 파일이 방어가 안 되는
상태라, 심사자가 찾으면 계층 건보다 인상이 나쁘다. 헤더 문구를 사실에 맞게 고쳐야 한다.

---

## 9. 남은 불일치 — coreset 크기

원 격자의 최적은 `cs100k` 인데 **정본은 `cs50k`** 다. 언제 왜 바뀌었는지 기록이 없다.
`d7_pc_sensitivity.json` 기준 L17_cs50k 0.9815 = L17_cs100k 0.9815 (동률)이라 성능상 무해하지만,
"격자에서 고른 값"과 "논문이 쓰는 값"이 다르다는 사실은 기록돼야 한다.

---

## 10. 권고 (저자 판단)

1. **§3.1.2 에 L17 근거를 되살리되 측정으로 뒷받침한다.** §6 의 표(val 꼬리)가 그 자리에 들어간다.
   삭제된 §4 문장을 그대로 복원하면 인용·측정 없는 단언으로 되돌아간다.
2. **배치는 §5 한계가 아니라 §3 본문 또는 재현성 부록.** 다만 "해결됐다"는 2/3 상태에서 세다 —
   "test 융합 성능으로 골랐고, 사후에 val 전용 기준(꼬리)으로 같은 계층이 나온다. 이 재확인은
   사후이며 CV 열에서는 L8 이 앞선다" 정도가 안전하다.
3. **부록 C 의 "이와 무관하게 고정되어 있다"** 를 사실에 맞게 고친다.
4. **`d7_pc_sensitivity.py` 헤더의 "provenance 방어"** 문구 정정.
5. **coreset 50k vs 100k** 불일치를 부록 A 재구축 세부에 한 줄로 적는다.

관련: `docs/260822_l17_coreset_provenance_results.md` · `docs/260531_performance_validation_protocol.md` ·
`reports/countgd/PREREG_branch_improvement.md`
