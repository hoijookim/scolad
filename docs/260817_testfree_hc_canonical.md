# hc × test-free 정본 수치 — 논문 일괄 교체용

> **260820 정정 통지**: 이 문서의 축별 std·범주별 표·§3 EAD-M 행이 260820 검수에서 정정됐다. 이미 조판했다면 교체 목록을 [`260820_paper_corrections.md`](260820_paper_corrections.md) 에서 확인할 것. **헤드라인 0.9723 ± 0.0011 은 불변이다.**
>
> **260821 추가**: §3 에 **PUAD-S 행이 신설**됐다 (0.9276 시드 1 → **0.9286 ± 0.0019** 시드 42/43/44). 캡션의 "별도 시드 집합" 표현도 정정 대상이다. 상세: [`260821_puad_s_3seed.md`](260821_puad_s_3seed.md).

작성: 2026-08-17 (GPU 세션) · 상태: **사전등록 완료, 확정**

논문 세션의 `c2fa80c` 커밋이 남긴 요청에 대한 답이다:
> 표 3·4·5 와 §4.5·4.7 의 수치가 아직 designed 프로토콜 값(0.9815 등)이다. …
> GPU 의 hc × test-free 사전등록 값이 확정되면 일괄 교체한다.

중첩 교차검증이 폐지되어 test-free 가 주 설정이 되었으므로, 아래 수치가 헤드라인이다.

---

## 1. 헤드라인 (권장)

**image-level AUROC (L+S) = 0.9723 ± 0.0011** (3-seed {42, 43, 44})
- logical 0.9685 ± 0.0024
- structural 0.9762 ± 0.0018

> **정정 이력**: 초기값 0.9720 은 EAD seed42 에서 **원본 모델의 test 점수 + 재학습 모델의
> val 점수**를 짝지은 상태였다(두 모델 상관 0.89~0.997, 동일하지 않음). z-정규화 기준이
> 다른 모델에서 오는 결함이다. 재학습 체크포인트로 seed42 test 를 다시 추론해 출처를
> 통일했고(원본 npz 는 `_backup_260615/ead_original_npz/` 에 보존), seed42 만
> 0.9713 -> 0.9721 로 바뀌고 43/44 는 불변이다(0.9714 / 0.9735) — 수정이 의도한 곳만
> 건드렸음을 확인했다. 재학습 seed42 값은 `_logs/ead_seed42_crosscheck.json` 의
> `repro` 항과 일치한다.

구성: `EfficientAD-S(재구성) + DINOv3-L L17 PatchCore(패치 메모리) + PSAD-hc(구성)`,
각 분기를 **공식 검증 분할의 정상 표본만으로 z-정규화**하고 **등가중 (1,1,1) 합**.
test 통계·라벨을 어느 단계에서도 쓰지 않는다.

**세 분기가 같은 시드로 함께 변동한다** — 이것이 이 수치를 권장하는 이유다. 아래 §2 의
변형은 EAD·PC 를 시드평균으로 고정해 PSAD 만 변동시키므로 분산이 과소평가된다.

### 시드별 / 범주별

| 시드 | L+S |
|---|--:|
| 42 | 0.9721 |
| 43 | 0.9714 |
| 44 | 0.9735 |

| 범주 | L+S |
|---|--:|
| breakfast_box | 0.9751 ± 0.0058 |
| juice_bottle | 0.9760 ± 0.0013 |
| pushpins | 0.9798 ± 0.0052 |
| screw_bag | 0.9642 ± 0.0034 |
| splicing_connectors | 0.9664 ± 0.0056 |

> 이 절의 축별·범주별 수치는 EAD seed42 출처 통일 **이후** 값이다. 2026-08-20 검수에서 위 정정 이력이 헤드라인에만 반영되고 축별 std·범주별 표에는 반영되지 않은 것을 발견해 산출물 JSON 에서 다시 읽어 채웠다 (pushpins 0.9786 → 0.9798 이 최대 변화).

산출물: `reports/countgd/testfree_final_eadfix_3seed.json`

---

## 2. 원본 프로토콜 정합 변형 (구 0.9751 과 동일 조건 비교용)

**L+S = 0.9733 ± 0.0007** / logical 0.9686 / structural 0.9779
시드별 42: 0.9735 / 43: 0.9738 / 44: 0.9725
범주별 break 0.977 / juice 0.976 / pushp 0.982 / screw 0.964 / splic 0.967

구 test-free 수치(0.9751 ± 0.0035)를 산출한 `scripts/countgd/val_znorm_fusion.py` 는
EAD 를 3시드 평균, PC 를 3-bank-seed 평균으로 **고정**하고 PSAD 만 시드를 바꿨다.
따라서 보고된 표준편차는 **PSAD 시드 변동만** 반영한다. 이 변형은 그 절차를 그대로 맞춘 것이다.

산출물: `reports/countgd/testfree_final_eadfix_origproto_3seed.json`

### 구 0.9751 과의 관계 — 통계적으로 구분되지 않는다

| | 평균 | 표준편차 | 시드별 |
|---|--:|--:|---|
| 구 수치 (hcp, 원본 파이프라인) | 0.9751 | 0.0035 | 0.9718 / **0.9788** / 0.9748 |
| 신 수치 (hc, 재구축) | 0.9733 | 0.0007 | 0.9735 / 0.9738 / 0.9725 |

차이 +0.0018 은 구 수치 자체 표준편차의 **0.51배**(1σ 이내), Welch p ≈ 0.5.
구 수치는 시드 하나(0.9788)가 평균을 끌어올린 구조이고 시드 폭이 0.0070 대 0.0014 로 **5배**다.
**중요**: 구 0.9751 은 **hcp**(구성분기 내부 PatchCore 포함)로 계산된 값이다 — 논문 헤드라인
분기 구성(hc)과 다르다. hc 의 test-free 수치는 이번에 처음 확정된 것이다.

---

## 3. 시드 집합을 {42, 43, 44} 로 통일했다 — **표 5 전체 완료**

논문 표기가 방법마다 달랐던 문제(주 설정 5-seed {42–46}, test-free 3-seed {0,42,1234},
SALAD 3, PUAD-S 1 …)를 정리했다. **전 방법이 {42,43,44} 로 재집계됐고 추가 학습은 필요 없었다.**

> **260821 정정**: 위 문장은 작성 시점에 **PUAD-S 만 참이 아니었다.** 표는 시드 44 단독값
> 0.9276 을 실은 채로 남아 있었다 — 집계 산출물 `ls_canonical_scores.json`(260604 커밋)이
> 시드 0·42·1234 점수(260612 생성)보다 먼저 굳었기 때문이다. 게다가 그 구 시드들은 체크포인트
> 출처가 서로 달랐고(특히 seed42 는 EAD-S 가 폐기한 `{cat}_seed42_v1` 경로), 해당 루트에
> 가중치가 남아 있지 않아 검증도 불가능했다. **{42,43,44} 를 현행 정본 루트(`_seeds_std`)에서
> 재채점해 0.9286 ± 0.0019 로 교체했다.** 상세: `260821_puad_s_3seed.md`.

| 방법 | 구 표기 | **{42,43,44}** |
|---|--:|--:|
| ScoLAD hc (구 주 설정, CV 프로토콜) | 0.9815 ± 0.0029 (5s) | **0.9807 ± 0.0009** |
| ScoLAD hcp (절제, CV 프로토콜) | 0.9796 ± 0.0023 (5s) | 0.9795 ± 0.0006 |
| **ScoLAD test-free hc (신 주 설정)** | — | **0.9723 ± 0.0011** |
| SALAD | 0.9469 ± 0.0074 (3s) | **0.9469 ± 0.0074** (동일) |
| PUAD-M | 0.9284 (3s) | **0.9284 ± 0.0042** |
| PUAD-S | 0.9276 (1s, 시드 44) | **0.9286 ± 0.0019** |
| EAD-S | 0.8966 ± 0.0027 (5s) | **0.8945 ± 0.0041** |
| ComAD | 0.7987 (3s) | **0.7987** (동일, std 산출 불가) |
| DINOv3-L PatchCore | 0.7901 ± 0.0056 (5s) | **0.7890 ± 0.0018** |
| EAD-M (full ImageNet) | 0.7846 (3s) | **0.8971 ± 0.0047** |
| ~~EAD-M (imagenette 폴백)~~ | 0.7846 (3s) | ~~0.7846 ± 0.0106~~ 교체됨 |

test-free 주 설정은 SALAD 재현 대비 **+2.54pp** 다.

> **260820 정정**: EAD-M 행을 교체했다. 구 값 0.7846 은 penalty 정칙화가 **imagenette 폴백**으로 학습된 것이라 EAD-S(full ImageNet)와 조건이 달랐고, 출판 순서(EAD-M 90.7 > EAD-S 90.0)를뒤집고 있었다. full ImageNet 판 0.8971 로 바꾸면 순서가 복원되고 PUAD-M 우위가 +14.4pp → **+3.1pp** 로 정직해진다. DINOv3-L 0.7890 은 메모리 뱅크에서 validation 을 제외한 train-only 재산출값이다. 상세: `260817_baseline_seed_audit.md`.

산출물: `reports/countgd/table5_3seed_42_43_44.json` (개별: `realign_*_3seed.json`)

### 작업 중 정정한 오판 2건 (기록)
- **EAD-M "seed42 만 보유, 6시간 학습 필요"는 오판이었다.** `efficient_ad_official_medium`
  (seed42 만)을 보고 표 5 가 실제로 쓰는 `efficient_ad_medium`(42/43/44 완비)을 놓쳤다.
  재집계하니 0.7846 ± 0.0106 으로 표 5 표기와 소수 4자리까지 일치한다.
- **SALAD "시드 변동이 존재하지 않는다"도 오판이었다.** `phase0/salad_reproduction` 의
  단일 실행 요약(저자 가중치 1회 추론, auc_combined 95.13)을 보고 판단했는데, 표 5 가 쓰는
  `path_y/salad_multiseed_v2/` 에는 시드 42/43/44 가 실제로 다른 값으로 있다(최대차 14.16).
  재집계하니 0.9469 ± 0.0074 로 표 5 표기와 일치한다. **표기 정정은 필요 없다.**
  원인: `phase0`(초기 단일 실행)과 `path_y`(표 5 다중시드)를 혼동했다.

### SALAD 수치 셋의 관계 — **정정 (2026-08-17)**

이 절에 처음 "96.1% 는 pooled image AUROC 이므로 L+S 0.9469 와 직접 비교할 수 없다"고
적었다. **그것은 내 오류였다.** 논문 세션이 원문 PDF 를 대조해 잡아냈다 — SALAD 원문
Table 4 는 `Det. Logical 96.5 / Det. Struct. 95.7 / Det. Avg 96.1` 이고 "the average for
both types is reported" 라고 명시한다. 즉 **96.1 은 논리·구조 동일 가중 평균으로 우리 L+S 와
같은 지표**이고 **직접 비교 가능**하다 ((96.5+95.7)/2 = 96.1).

| 값 | 정체 | 우리 L+S 와의 관계 |
|--:|---|---|
| 96.1% | 원논문 발표값 = 논리·구조 동일 가중 평균 | **직접 비교 가능** |
| 0.9469 | 우리 3시드 자체 학습 L+S {42,43,44} | 격차 **−1.4%p** |
| 95.13% | 저자 공개 가중치 1회 추론 `auc_combined` (초기 검증용) | 지표가 달라 참고만 |

**동일 지표 재현 격차는 −1.4%p 다.** 논문 세션이 함께 교정한 다른 인용값
([8] ComAD 0.898 → **0.812** — 0.898 은 AST+ComAD 열이었다, [26] PUAD 0.931 은 PUAD-**S** 값이고
PUAD-M 은 92.23)까지 반영하면 동일 지표 재현 격차가 EAD-M −1.0 / EAD-S −0.5 / ComAD **−1.3** /
SALAD −1.4%p 로 **전부 1.4%p 이내**다.

(ComAD 격차는 L+S 기준이다: 출판 0.812 − 재현 0.7987 = −1.33pp. 앞서 −0.5pp 로 적었던 것은
pooled 0.8067 을 쓴 결과이고 L+S 자리에 pooled 를 넣은 내 착오였다.) 재현 충실성을 근거로 동일 환경 비교를 주장할 수 있다.

(95.13% 는 저자 가중치 1회 추론값이고 그 −0.97pp 격차는 breakfast_box 하나에서 온다 —
원논문 full ImageNet teacher 대신 Imagenette 로 대체해 그 범주만 84.0%, −10pp. 초기 검증용
산출물이며 표 5 의 원천이 아니다.)

## 4. hc 구성이 hcp 보다 낫다 — 등가중 조건에서도 성립

| 구성 | hc | hcp | hc − hcp |
|---|--:|--:|--:|
| 병합만 | 0.9707 ± 0.0025 | 0.9689 ± 0.0042 | +0.18pp |
| 병합+TTA | 0.9720 ± 0.0012 | 0.9702 ± 0.0021 | +0.10pp |

(위 두 행은 EAD 출처 통일 전 값이다. hc·hcp 를 같은 조건에서 비교하므로 부호·크기는
그대로 유효하다. 통일 후 확정 hc 는 0.9723 ± 0.0011 이다.)

논문의 CV 프로토콜 절제(+0.19pp)와 방향이 같다. **가중치 재조정이 없는 등가중 고정
조건에서도 성립**한다는 것이 새 근거다 — p 항이 독립 PatchCore 분기와 중복이라는
260630 설계 판단이 test-free 조건에서도 유효하다. screw_bag 에서 특히 크다
(hc 0.965 vs hcp 0.957).

---

## 5. 이 수치에 이르기까지의 개선과 사전등록

재구축 기준선 0.9650 → **0.9723** (+0.73pp). 채택 2건, 기각 8건.
설계 결정은 전부 test 를 보기 전에 확정했고 근거는 train/val 뿐이다.
상세: `reports/countgd/PREREG_branch_improvement.md`

**채택**
- **E5 혼동쌍 자동 병합**: train 의사레이블 vs UNet train 예측의 혼동행렬에서
  C[i,j]+C[j,i] > 0.10 인 쌍을 병합. 실측에서 screw_bag c4↔c5 만 걸린다
  (세 시드 0.168 / 0.261 / 0.142, 차순위 전 범주 ≤ 0.012 — 10배 격차). 범주별 수작업 없음.
  screw_bag val 점수 꼬리 22.21 → 2.79.
- **E7 회전 TTA (screw_bag 한정)**: 학습 증강 범위와 동일한 각도(screw_bag 은 (0,360)
  학습이라 90도 배수 4개). 3시드 val 꼬리 평균 5.34 → 3.67, 최대 9.17 → 4.91.
  규칙(평균·최댓값을 둘 다 줄일 것)을 5범주에 동일 적용해 screw_bag 만 통과.

**기각** (수치가 높아도 되살리지 않았다)
E1 standardize=0 · E2 표현 확장(V4~V7) · E4 소프트 마스크(pushpins val 꼬리 1.77→10.03)
E6 레이블 전역 정준화(면적 CV 0.375→1.303) · E8 k-NN k=5(splicing val 6.92→24.88)
E8b h:c 가중 w_c=0.25 · E8c 마할라노비스/전경비율 · E9 개수 거리(screw val 3.67→14.10)
UNet 재학습(천장이 CSAD 레이블)

**E8b 기각 사유는 논문에 쓸 만하다**: 기준 평균 +0.0150 으로 최대 개선이었으나 기각했다.
그 기준은 마스크를 조작해 이상을 모사하므로 h(면적)는 직접, c(외형)는 간접으로만 변한다
— 구조적으로 c 를 과소평가한다. 마스크 기준으로 c 가중을 깎는 것은 근거가 없다.

---

## 6. 방법론적 부산물 — 논문에 실을 만한 것

**외형 섭동 기준을 신설**해 지금까지 측정 불가였던 c 항을 열었다(마스크는 그대로 두고
클래스 픽셀만 recolor/desat/texswap). 결과: c 항은 전 범주에서 사실상 포화다.

| 범주 | recolor | desat | texswap |
|---|--:|--:|--:|
| breakfast_box | 1.0000 | 0.9996 | 1.0000 |
| juice_bottle | 1.0000 | 0.9591 | 1.0000 |
| pushpins | 0.9896 | 0.9998 | 1.0000 |
| screw_bag | 0.9955 | **0.6882** | 1.0000 |
| splicing_connectors | 1.0000 | 1.0000 | 1.0000 |

screw_bag desat 만 낮은데, 나사·너트가 원래 금속 무채색이라 회색조로 바꿔도 거의 변하지
않는 **양성 미탐지**다. 이로써 juice_bottle 격차를 "c 항이 외형 이상에 약해서"로 본 가설이
기각됐다(hcp 로 재도 juice 0.977 이라 p 항 문제도 아니다).

**부품 분해 실측** — CSAD 의사레이블의 상류 한계:

| 범주 | 클래스 | 성분(≥200px) | 물리 부품(대략) |
|---|--:|--:|---|
| pushpins | 15 | 15.0 | 15 ✓ |
| screw_bag | 5 | 4.4 | 7 |
| breakfast_box | 6 | 2.5 | 6–7 |
| juice_bottle | 5 | 1.0 | 4 |
| splicing_connectors | 2 | 1.0 | 3 |

juice_bottle·splicing 은 전경이 통째로 한 덩어리이고 부품 구분은 클래스 분할로만 표현된다.
이는 마스크 기반 기준의 타당성 한계도 뜻한다 — 그 범주에서 "성분 하나 제거"는 물체 전체
제거와 같아 자명하게 탐지되므로, 1.0000 포화는 분기 성능이 아니라 **섭동의 퇴화**다.

---

## 7. 산출물 경로

| 내용 | 파일 |
|---|---|
| **헤드라인 (엄격, EAD 출처 통일)** | `reports/countgd/testfree_final_eadfix_3seed.json` |
| **원본 정합 변형 (동일)** | `reports/countgd/testfree_final_eadfix_origproto_3seed.json` |
| (구) EAD 출처 혼재 상태 | `reports/countgd/testfree_corrected_3seed.json` 등 |
| hcp 절제 | `reports/countgd/testfree_final_hcp_3seed.json` |
| 시드 재집계 | `reports/countgd/realign_3seed_42_43_44.json` |
| 사전등록 전문 | `reports/countgd/PREREG_branch_improvement.md` |
| 외형 기준 | `reports/countgd/criterion_appearance.json` |
| 재구축 격차 진단 | `scripts/psad_rebuild/diagnose_rebuild_gap.py` |

---

## 8. 보충 산출 4종 (논문 세션 미완 목록 대응, 2026-08-17)

전부 **확정 설정**(hc + 혼동쌍 병합 + screw_bag TTA / 검증 정상 z-정규화 + 등가중 합)에서
시드 {42,43,44} 로 산출한 **보고용 수치**다. 설정을 고르는 데 test 를 쓰지 않았다.
산출물: `reports/countgd/testfree_supplements_3seed.json`,
스크립트: `scripts/psad_rebuild/testfree_supplements.py`

### 8.1 표 3 — 융합 방법 비교

| 방법 | L+S | Δ vs 주 설정 |
|---|--:|--:|
| **주 설정 (val z-정규화 + 등가중 합)** | **0.9723 ± 0.0011** | — |
| 최고 단일 분기 (구성 PSAD-hc) | 0.8948 ± 0.0071 | **−7.75pp** |
| raw-mean (정규화 없음) | 0.8116 ± 0.0031 | **−16.07pp** |

구 표기(5-seed, CV 프로토콜)의 raw→주설정 +17.8pp / 최고단일 +1.06pp 와 비교하면
**주장 방향은 유지되고 "융합이 최고 단일 신호원을 능가"는 오히려 크게 강해진다**
(+1.06pp → +7.75pp). CV 프로토콜에서는 가중치 최적화가 단일 분기도 끌어올렸으나,
등가중 고정에서는 그 여지가 없어 융합 이득이 선명해진다.

**RandomForest 행은 산출하지 않았다.** 학습형 융합은 라벨된 이상 표본을 요구하고,
중첩 교차검증 폐지로 논문에 test 를 보는 절차가 0 이 된 상태에서는 test-free 프로토콜로
계산할 방법이 없다. 표 3 에서 그 행을 빼거나 "라벨 확보 시 참고치(프로토콜 다름)"로
명시해야 한다 — 없는 수치를 만들지 않았다.

### 8.2 §4.5 — 다중 지표 (주 설정, 3-seed)

| 지표 | 값 |
|---|--:|
| AUROC (L+S) | **0.9723 ± 0.0011** |
| AUPR | **0.9797 ± 0.0002** |
| 최대 F1 | **0.9485 ± 0.0030** |
| logical | 0.9685 ± 0.0024 |
| structural | 0.9762 ± 0.0018 |

### 8.3 §4.6 — 쌍별 조합 (같은 프로토콜, 등가중)

| 조합 | L+S | 3소스 대비 |
|---|--:|--:|
| EAD + PC (구성 제외) | 0.9023 ± 0.0046 | **−7.00pp** |
| PC + 구성 | 0.9497 ± 0.0017 | −2.26pp |
| EAD + 구성 | 0.9633 ± 0.0006 | −0.90pp |
| **3소스 (주 설정)** | **0.9723 ± 0.0011** | — |

순서가 논문 서술과 같다. 핵심 논거인 "구성 분기를 뺀 조합이 가장 나쁘다"가 그대로 성립하며,
등가중 조건에서 그 격차가 −7.00pp 로 뚜렷하다.

### 8.4 표 5 — 분기별 단독 열 (3-seed)

| 분기 | L+S | break | juice | pushp | screw | splic |
|---|--:|--:|--:|--:|--:|--:|
| **구성 (PSAD-hc)** | **0.8948 ± 0.0071** | 0.871 | 0.892 | 0.914 | **0.939** | 0.858 |
| EAD-S (재구성) | 0.8945 ± 0.0041 | 0.822 | 0.982 | 0.978 | 0.732 | 0.958 |
| DINOv3-L PatchCore | 0.7890 ± 0.0018 | 0.861 | 0.868 | 0.710 | 0.694 | 0.812 |
| **3소스 융합 (주 설정)** | **0.9723 ± 0.0011** | 0.975 | 0.976 | 0.980 | 0.964 | 0.966 |

**구성 분기가 단독 최고**다(0.8948 > EAD-S 0.8945). screw_bag 에서 0.939 대 0.732 로
압도하며, 이는 논문의 "구성 기여 screw_bag +23.96pp" 서술과 방향이 같다. 세 분기가
서로 다른 범주에서 강한 것(EAD-S: juice·pushp·splic / 구성: screw·pushp / PC: break)이
융합 이득의 근거다.
